# Datos locales (resultados de escaneo, caché geo, notas, logs)
# NUNCA se suben al repo: se guardan solo en tu PC.
data/

# Binaries (anclados a la raíz: /dashboard es el binario, no cmd/dashboard)
*.exe
*.exe~
/netscanner
/dashboard
/enrich
/.dashboard.pid

# Scan results and data files (fuera de data/ por precaución)
*.jsonl
*.csv
scan_stats.json
*.stats
GeoLite2-*.mmdb

# Local Tor bundle (installed by anon.ps1)
tools/

# Logs
*.log

# Notas locales y clave de IA (nunca se suben)
comments.json
ai_key.json

# Caché de geolocalización local (IPs y ubicaciones tuyas)
geo-cache.json

# OS
.DS_Store
Thumbs.db
